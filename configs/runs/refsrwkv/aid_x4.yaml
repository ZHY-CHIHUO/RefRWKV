base:
  - ../../common/refsrwkv.yaml
  - ../../models/refsr/refsrwkv.yaml

dataset:
  config: ../../datasets/sr/aid.yaml

run:
  name: aid_x4
  scale: 4
  lr_patch: 48

data:
  reference_mode: none
  batch_size: 16
  val_batch_size: 1
  max_samples_val: 300

model:
  use_reference: false

loss:
  ssim_weight: 0.0
  fft_weight: 0.0
