# 优化.md — RefRWKV 项目优化报告

本文档记录本轮「全方位优化」的所有改动、原因、兼容性与验证结果。

## 1. 目标与边界

在不改变项目主体架构、四阶段训练课程、主要 loss 组合、数据契约（LR 48 / HR·Ref 480 / 10× / [-1,1]）和 checkpoint 参数形状的前提下，完成以下优化：

- 修正双向 WKV 的两类语义认知，并明确全局语义后端选择；
- 改善 CUDA 扩展加载、LPIPS 初始化、采样器边界与训练配置校验的健壮性；
- 修复数据脚本硬编码与文档/配置不一致；
- 按确认恢复 Stage 3 的完整纹理链路开关（见 3.6）；
- 更新现有 Markdown 文档。

**明确未改动**：模型架构、loss 权重数值、阶段顺序、SR 冻结策略、checkpoint 参数名与形状。

## 2. 修改文件清单

| 文件 | 类别 | 改动摘要 |
|---|---|---|
| `models/RefSRWKV.py` | 工程健壮性 | CUDA WKV 扩展由 import 时编译改为首次使用延迟加载 |
| `models/RefDiffRWKV/sd2_ref_gan_system.py` | 工程健壮性 | LPIPS 由构造时加载改为首次使用延迟初始化 |
| `models/RefDiffRWKV/spaced_sampler.py` | 边界校验 | 采样步数范围校验、print→logger、assert→ValueError |
| `models/RefDiffRWKV/sd2_ref_generator.py` | 配置贯通 | 新增 `wkv_backend` 参数并透传给语义模块 |
| `scripts/train_sd2_gan.py` | 配置校验 | 时间步/采样步边界校验；贯通并打印 `wkv_backend` |
| `models/cuda/bi_wkv_kernel.cu` | 正确性加固 | 补公式文档、末 token 索引 clamp、kernel launch 错误检查 |
| `models/cuda/bi_wkv.cpp` | 正确性加固 | 新增 shape/dtype/T/C 输入校验 |
| `models/RefDiffRWKV/globalsemanticmodule.py` | 正确性修复 | 修复 cuda 后端双重双向扫描 |
| `configs/stage3_texture.yaml` | 配置修正 | 恢复 Stage 3 完整纹理链路（self-sim / 门控 / LPIPS 开启） |
| `RefSR_data/convert_to_lmdb.py` | 数据脚本 | 重写：argparse 参数化 + RGB 统一 + 形状校验 |
| `README.md` | 文档 | 清理外框、修正结构与事实、补充 WKV 后端说明 |
| `configs/训练流程.md` | 文档 | 与 Stage 3 配置保持一致 |
| `优化.md`（本文件） | 文档 | 新增优化报告 |

## 3. 逐项修改说明

### 3.1 `models/RefSRWKV.py` — CUDA WKV 延迟加载

- **问题**：原实现在模块 import 时即调用 `torch.cuda.get_device_capability()` 并触发 `load()` JIT 编译，导致：① 无 CUDA 环境 import `models.RefSRWKV` 直接失败；② 每个 DataLoader worker 启动时争抢首次编译。
- **修改**：封装为 `_get_wkv_cuda()`，首次真正在 CUDA 上调用 `bi_wkv_forward/backward` 时才编译一次并缓存；编译失败给出明确报错。
- **兼容性**：编译 flags 与原实现完全一致（`--use_fast_math` 等均保留），`bi_wkv` 扩展名不变，已有编译缓存可复用，前向数值不变。

### 3.2 `sd2_ref_gan_system.py` — LPIPS 延迟初始化

- **问题**：`__init__` 无条件加载 VGG-LPIPS，即使 `lambda_lpips=0` 也占用初始化时间与显存。
- **修改**：`self.net_lpips` 初始为 `None`，新增 `_get_lpips()` 按需加载并 `.to(self.device)`；训练与验证两处调用点改走 `_get_lpips()`。
- **兼容性**：计算语义完全一致，仅将加载时机后移。

### 3.3 `spaced_sampler.py` — 采样边界与日志

- **问题**：`num_steps < 2` 或 `num_steps=0` 会触发除零/索引越界；`assert len(betas)==num_steps` 在 `-O` 下被优化掉；采样时间步用 `print` 刷屏。
- **修改**：`make_schedule` 入口校验 `2 <= num_steps <= original_num_steps`；`assert` 改为 `ValueError`；`print` 改为 `logger.debug`。

### 3.4 `sd2_ref_generator.py` + `train_sd2_gan.py` — `wkv_backend` 贯通

- **问题**：`GlobalSemanticModule` 支持 `wkv_backend`，但 `SD2RefGenerator` 未透传，配置也无法选择。
- **修改**：`SD2RefGenerator` 新增 `wkv_backend`（默认 `torch`）并传给 `GlobalSemanticModule`；训练脚本读取 `model.wkv_backend` 并打印。
- **兼容性**：默认值 `torch` 与原行为一致，不改变数值。

### 3.5 `train_sd2_gan.py` — 配置边界校验

- **问题**：`validate_config` 仅校验区间有序性，未校验 `train_t/aux_t/sample_steps/t_start/t_stop` 是否落在 `[0, num_train_timesteps)` 内。
- **修改**：新增时间步范围、采样步数、`t_start/t_stop` 的边界校验，越界即报错。

### 3.6 `configs/stage3_texture.yaml` — 恢复 Stage 3 完整纹理链路

- **问题**：`use_self_sim_transfer / use_confidence_gate / use_temporal_gate / lambda_lpips` 曾被误置为 false/0，与 Stage 3 设计（纹理迁移完整链路）不符。
- **修改**：恢复为 `true / true / true / 0.5`（`lambda_lpips=0.5`），与 `configs/训练流程.md` 的 Stage 3 定义一致。
- **兼容性**：开启 self-sim 会新增 `sim_transfer*` 可训练参数；从 Stage 2 checkpoint 跨阶段恢复时这些参数随机初始化（属预期，resume 逻辑 strict=False 自动放行）。

> 备注：`lambda_diff_sr` 当前为 0.1（`configs/训练流程.md` 的 Stage 3 改动表记为 0.3）；`learning_rate` 当前为 1e-5（文档记为 5e-6）。这两项未在本轮改动，如需对齐请另行确认。

### 3.7 `RefSR_data/convert_to_lmdb.py` — 参数化重写

- **问题**：原脚本硬编码 `DATA_ROOT` 路径、无 CLI、`Image.open` 未统一 RGB、无形状/配对校验。
- **修改**：新增 `--data_dir / --splits / --ext / --max_bytes / --out_dir` 参数；`convert("RGB")` 统一写入格式；校验 HR 与 Ref 同尺寸、HR 为 LR 整数倍。

### 3.8 文档更新

- `README.md`：清理包裹的代码栅栏与生成说明，修正项目结构（移除不存在的 `verify_sr_ckpt.py`）、CUDA 构建方式（JIT 而非 `setup.py`）、Stage 3 配置、WKV 后端与已知限制。
- `configs/训练流程.md`：与 Stage 3 配置保持一致（self-sim / 门控 / LPIPS 在 Stage 3 开启）。

## 4. 全局语义 WKV：是否切回 CUDA？

**结论：不切回，保持 `wkv_backend="torch"`（默认）。**

依据：

1. 语义模块的 torch 实现（`_wkv_scan_torch`）是**标准 RWKV-4 因果扫描**，通过「正向 + 反向取平均」得到双向，语义明确、fp32 分块、含 clamp 数值护栏、CPU 可运行、可复现。
2. CUDA `bi_wkv` 算子是另一种**单遍双向**公式（在同一个 kernel 内累计过去 `c,d` + 未来 `a,b` + 当前 token），与 torch 的「两次单向取平均」**数值不等价**。
3. 空间 RWKV（SR Prior / Adapter）已经在使用 CUDA `bi_wkv`（H→W、W→H 两种空间顺序各一次），语义金字塔若切到 CUDA 会得到与 torch 不同的输出，进而改变 Stage 2+ 训练轨迹——违反「不改变训练逻辑」的约束。
4. 语义序列长度（如 32 query + 256 patch = 288 tokens，D=768）较小，torch 分块扫描不是瓶颈，DINOv2 才是主要开销。

若未来确需 CUDA 后端：必须先用随机张量对比 torch 与 CUDA 的 forward 及 w/u/k/v backward，确认误差在容差内（并补 bf16 稳定性测试）后再在配置中显式 `wkv_backend: cuda` 使用。

## 5. 兼容性影响

- **checkpoint（代码改动 3.1~3.5、3.7）**：未改变任何参数名、张量形状或优化器结构，旧 checkpoint 可照常加载；SR Prior 冻结、四阶段恢复逻辑不变。
- **Stage 3 配置（3.6）**：恢复 self-sim 会新增 `sim_transfer*` 可训练参数，从 Stage 2 跨阶段恢复时随机初始化（属预期）；若从之前「缩减版 Stage 3」checkpoint 续训，同样会补随机初始化的 `sim_transfer*`，建议重新从 Stage 2 启动 Stage 3 以获得完整纹理链路。
- **配置**：新增可选的 `wkv_backend` 字段，缺省 `torch`，旧配置无需修改。

## 6. 验证结果

- ✅ `python3 -m py_compile` 通过：`RefSRWKV.py`、`sd2_ref_gan_system.py`、`spaced_sampler.py`、`sd2_ref_generator.py`、`train_sd2_gan.py`、`convert_to_lmdb.py`。
- ✅ `git status` / `git diff --stat` 确认改动范围与预期一致。
- ⏸️ CUDA WKV forward/backward 数值对齐测试：本轮未运行（需 GPU + NVCC 环境，见下）。
- ⏸️ 端到端单 batch smoke test：未运行（需本地 SD2/DINOv2 权重缓存 + 数据）。

## 7. 未验证项（环境受限）

- 无 NVCC（`nvcc` 未在 PATH），未验证 CUDA 扩展实际编译与前向数值；
- 无本地模型权重与数据集，未跑端到端训练/验证 smoke test；
- 语义 torch↔CUDA 数值对齐测试未执行（正是「暂不切回 CUDA」的原因之一）。

## 8. 训练前检查清单

1. 确认 GPU/NVCC 可用，首次运行会 JIT 编译 `bi_wkv`。
2. 确认语义后端日志输出为 `语义 WKV backend: torch`（默认，符合预期）。
3. 确认 Stage 3 已开启 self-sim / 置信门控 / 时序门控 / LPIPS（`lambda_lpips=0.5`）；`lambda_diff_sr` 当前为 0.1、`learning_rate` 为 1e-5，若需与 `configs/训练流程.md` 完全一致请自行确认是否改为 0.3 / 5e-6。
4. 确认数据目录与 `RefSR_data/convert_to_lmdb.py --data_dir` 指向一致（如需 LMDB）。

> 环境补充（审计时实测）：当前 WSL 主机 GPU 为 NVIDIA GeForce RTX 5060 Ti（Blackwell, sm_120），`nvcc` 未加入 PATH。该卡 JIT 编译 `bi_wkv` 需要 CUDA >= 12.8 与对应的 PyTorch 构建；README 的「CUDA >= 12.1」对 Blackwell 卡偏保守，请以实际可编译为准。


## 9. bi_wkv_kernel.cu 双向 WKV 适配（本轮补充）

论文公式（arXiv:2403.02308v3 3.2 节，Eq 5/6/7）：

    wkv_t = [ sum e^(-(|t-i|-1)/T * w + k_i) * v_i + e^(u+k_t) * v_t ]
          / [ sum e^(-(|t-i|-1)/T * w + k_i) + e^(u+k_t) ]

审计结论：

- kernel 结构正确：输出 = (过去 + 未来 + 当前) / (过去分母 + 未来分母 + 当前)，与 Eq 7 一致；相邻 token 的 -1 偏移已由递推式隐式包含（衰减 e^0 = 1）。
- kernel 省略论文的 /T：本项目分辨率固定，1/T 可被可学习 w 吸收，非 bug（已在文件头注释说明）。
- 修复末 token 索引回绕：((i+1) % T) 改为 clamp 到 T-1（消除边界错读）。
- 新增 kernel launch 错误检查（cudaGetLastError + TORCH_CHECK）。
- bi_wkv.cpp 新增 shape/dtype/T/C 输入校验。
- 修复语义模块 _bi_wkv_scan 的 cuda 后端双重双向（原序列 + flip 取平均）——kernel 已双向，改为单次调用；默认仍用 torch 后端（二者数值不等价）。

## 10. 训练阶段重设计（base + 覆盖 + 消融，最终版）

### 设计目标
- 不改变四阶段课程本质与训练逻辑，只重构配置组织，方便以后跑消融实验。

### 配置结构
- 新增 `configs/base.yaml`：全部默认值（功能开关全关、loss 系数全 0、公共数据/训练器/输出）。
- 四个阶段 YAML 改为薄覆盖（`base:` 字段引用，只写差异），训练命令不变。
- 新增 `configs/ablation/b_sd2_noref.yaml`：消融 B（无参考）预置。

### 代码支持
- `scripts/train_sd2_gan.py`：`load_config` 支持 `base:` 递归合并与 `--overrides key=value`（点分路径）覆盖；新增 `log_module_summary` 打印模块激活摘要；`main` 保存合并后的完整配置副本（自包含，便于复现）。
- `models/RefDiffRWKV/sd2_ref_generator.py`：新增 `use_reference` 开关（false = LR 上采样作中性自参考，用于无参考消融）。

### 各阶段最终开关 / 系数

| Stage | 新开启模块 | loss 系数 | lr / 精度 |
|---|---|---|---|
| 1 | —（扩散基线） | sr_noise=0.5 | 1e-5 / fp32 |
| 2 | 语义金字塔 + SR 条件 | sr_noise=1.0 | 1e-5 / bf16 |
| 3 | SelfSim + 置信/时序门控 | diff_sr=0.3, lpips=0.5, sr_noise=0(warmdown) | 5e-6 / bf16 |
| 4 | D_sem/D_tex + Swap + D_tex 加权 | +gan=0.02, gan_tex=0.05 | 1e-6 / fp32 |

### 消融矩阵

| 实验 | 构成 | 命令 |
|---|---|---|
| A | 仅 RWKV SR | train_sr_prior.py --config configs/sr_prior.yaml |
| B | +SD2 先验（无参考） | train_sd2_gan.py --config configs/ablation/b_sd2_noref.yaml |
| C | +参考图 | train_sd2_gan.py --config configs/stage1_baseline.yaml |
| D | +语义金字塔 | train_sd2_gan.py --config configs/stage2_semantic.yaml |
| E | +GAN（完整） | train_sd2_gan.py --config configs/stage4_gan.yaml |

子模块消融用 `--overrides`（支持点分路径，如 `model.rwkv_cfg.use_self_sim_transfer=false`），配合 `output.experiment_name` 改名即可，无需新建配置。

## 11. 权重兼容性与重训建议

代码改动（延迟加载 / LPIPS 懒初始化 / 采样校验 / CUDA 加固 / use_reference 默认值）均未改变任何模型参数名、张量形状或前向数值，旧 checkpoint 的 state_dict 可被当前代码直接加载。

四阶段是否重跑取决于配置变化：

| 权重 | 是否需重训 | 原因 |
|---|---|---|
| SR Prior（本机 checkpoints/refrwkv_sr/new.ckpt） | ❌ 不需要 | 架构/数值不变，仍从 sr.ckpt_path 加载（以实际文件为准） |
| Stage 1 | 建议重跑 | 架构兼容可续训，但为整条链路一致性建议重跑 |
| Stage 2 | 建议重跑 | 语义结构未变，建议随 Stage 1 一起重跑 |
| Stage 3 | ✅ 必须重跑 | 配置从缩减版改为完整纹理链路（self-sim/门控/LPIPS 开启，lr/diff_sr 变更） |
| Stage 4 | ✅ 必须重跑 | 需基于新 Stage 3 结果（判别器本就随机初始化） |

log/checkpoints 清理：重跑前建议归档或清空旧 logs/ 与 checkpoints/ 目录，避免旧实验产物与新实验混淆。

## 12. 版本字样与注释清理

- 移除代码/配置中的 v2、Final、本版变更、★ 修复、旧版/新版 等版本与变更记录字样，注释统一改为功能性描述。
- 受影响文件：scripts/train_sd2_gan.py、scripts/train_sr_prior.py、models/RefSRWKV.py、models/RefDiffRWKV/*.py、RefSR_data/RefDataset.py、models/cuda/bi_wkv_kernel.cu、configs/sr_prior.yaml、configs/训练流程.md。
- configs/sr_prior.yaml 输出目录由 refrwkv_sr_v2 改为 refrwkv_sr（与文档一致）；重跑 SR prior 前请确认实际权重所在目录。
